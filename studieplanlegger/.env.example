# Browser address: http://127.0.0.1:8088
STUDIEPLAN_PORT=8088

# Optional and empty-database-only. Keep false for a normal empty start. If
# enabled, provide a complete validated v1 envelope on one JSON line.
STUDIEPLAN_SEED=false
STUDIEPLAN_SEED_JSON={"schemaVersion":1,"tasks":[]}
